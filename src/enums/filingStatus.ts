export enum FilingStatus {
  CANCELLED = 'CANCELLED',
  COMPLETED = 'COMPLETED',
  CORRECTED = 'CORRECTED',
  DELETED = 'DELETED',
  DRAFT = 'DRAFT',
  ERROR = 'ERROR',
  NEW = 'NEW', // used for Todo List items only (ie, not yet a filing)
  PAID = 'PAID',
  PENDING = 'PENDING',
  WITHDRAWN = 'WITHDRAWN',
}
